'use client';

import Temp from './temp';
import { Suspense } from 'react';

const ProfileEditPage = () => {
  return (
    <Suspense>
      <Temp />
    </Suspense>
  );
};

export default ProfileEditPage;
